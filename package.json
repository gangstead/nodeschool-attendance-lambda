{
  "name": "webhook-lambda",
  "version": "1.0.0",
  "repository": "blahblahblah",
  "description": "Lambda function that acts as a Github webhook",
  "main": "index.js",
  "scripts": {
    "build": "zip -r nodeschool-attendance-lambda.zip *",
    "clean": "rm -f nodeschool-attendance-lambda.zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Gangstead",
  "license": "ISC",
  "dependencies": {
    "googleapis": "^26.0.1",
    "lodash": "^4.17.5",
    "moment": "^2.20.1",
    "request": "^2.83.0",
    "request-promise": "^4.2.2"
  }
}
